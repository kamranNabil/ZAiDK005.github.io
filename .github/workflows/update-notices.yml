name: Update Notice Counts

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          pip install -r .github/scripts/requirements.txt
          npx playwright install-deps chromium || true
          python -m playwright install chromium

      - name: Fetch live notice counts
        run: python .github/scripts/update_notices.py

      - name: Commit updated notices.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add notices.json
          git diff --staged --quiet || git commit -m "chore: auto-update notice counts via Playwright"
          git push
